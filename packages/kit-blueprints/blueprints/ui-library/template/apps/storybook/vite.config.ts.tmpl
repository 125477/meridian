import { defineConfig } from "vite";
{{#if isVue}}
import vue from "@vitejs/plugin-vue";
{{else}}
import react from "@vitejs/plugin-react";
{{/if}}

export default defineConfig({
  plugins: [{{#if isVue}}vue(){{else}}react(){{/if}}],
});
