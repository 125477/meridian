{
  "name": "@{{projectName}}/electron",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsc -p tsconfig.json && node ./scripts/wait-for-web.mjs && electron .",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "electron": "^34.0.0",
    "typescript": "^5.7.2"
  }
}
