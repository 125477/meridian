{
  "name": "{{projectName}}",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @{{projectName}}/docs dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint"
  }
}
