{
  "name": "{{projectName}}",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @{{projectName}}/web dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint"
  }
}
